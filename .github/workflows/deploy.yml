name: Build Sentinel

on:
  workflow_call:
    inputs:
      HEROKU_APP_NAME:
        required: true
        type: string

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      # - uses: akhileshns/heroku-deploy@v3.12.12
      #   with:
      #     heroku_app_name: ${{ inputs.HEROKU_APP_NAME }}
      #     heroku_api_key: ${{ secrets.HEROKU_API_KEY }}
      #     heroku_email: "quinnstoltz@gmail.com"
      #     justlogin: true
      # - run: heroku auth:whoami
      # - name: Login to Heroku Container registry
      #   run: |
      #     heroku auth:login 
      #     heroku auth:whoami
      #   env: 
      #     HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
      # - run: heroku auth:whoami
      #   env: 
      #     HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
      # - run: heroku login
      #   env: 
      #     HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
      - name: Create remote
        run: |
          heroku git:remote --remote=heroku --app=${{ inputs.HEROKU_APP_NAME }}
          git branch -r
        env: 
          HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
      - run: git push heroku
        env: 
          HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
